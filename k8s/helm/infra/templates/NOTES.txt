This infra chart defined the following:
- cert-manager issuers for letsencrypt and selfSigned
- API gateways {{- range .Values.gateways }} {{ .name }}{{ end }}
- daily k8s backup to {{ .Values.k8sBackup.destPath }}
- default CPU and memory limits for {{ .Values.namespace }} namespace
- local-storage class default
- cluster role permissions for prometheus
- node port {{ .Values.rsyslogExt.nodePort }} for rsyslog
