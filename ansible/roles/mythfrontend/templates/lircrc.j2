{{ ansible_managed | comment }}
# LIRCRC Auto Generated by Mythbuntu Lirc Generator
# Author(s): Mario Limonciello, Nick Fox
# Created for use with Mythbuntu
# Adapted by richb

begin
    remote = mceusb
    prog = irxevent
    button = Seven
    config = Key 7 CurrentWindow
    repeat = 0
    delay = 0
end

begin
    remote = mceusb
    prog = irxevent
    button = Right
    config = Key Right CurrentWindow
    repeat = 0
    delay = 0
end

begin
    remote = mceusb
    prog = irxevent
    button = Mute
    config = Key | CurrentWindow
    repeat = 0
    delay = 0
end

begin
    remote = mceusb
    prog = irxevent
    button = Skip
    config = Key End CurrentWindow
    repeat = 0
    delay = 0
end

begin
    remote = mceusb
    prog = irxevent
    button = Replay
    config = Key Home CurrentWindow
    repeat = 0
    delay = 0
end

begin
    remote = mceusb
    prog = irxevent
    button = One
    config = Key 1 CurrentWindow
    repeat = 0
    delay = 0
end

begin
    remote = mceusb
    prog = irxevent
    button = Down
    config = Key Down CurrentWindow
    repeat = 0
    delay = 0
end

begin
    remote = mceusb
    prog = irxevent
    button = Zero
    config = Key 0 CurrentWindow
    repeat = 0
    delay = 0
end

begin
    remote = mceusb
    prog = irxevent
    button = Home
    config = Key M CurrentWindow
    repeat = 0
    delay = 0
end

begin
    remote = mceusb
    prog = irxevent
    button = Pause
    config = Key P CurrentWindow
    repeat = 0
    delay = 0
end

begin
    remote = mceusb
    prog = irxevent
    button = Six
    config = Key 6 CurrentWindow
    repeat = 0
    delay = 0
end

begin
    remote = mceusb
    prog = irxevent
    button = Two
    config = Key 2 CurrentWindow
    repeat = 0
    delay = 0
end

begin
    remote = mceusb
    prog = irxevent
    button = ChanDown
    config = Key Down CurrentWindow
    repeat = 0
    delay = 0
end

begin
    remote = mceusb
    prog = irxevent
    button = ChanUp
    config = Key Up CurrentWindow
    repeat = 0
    delay = 0
end

begin
    remote = mceusb
    prog = irxevent
    button = Rewind
    config = Key Left CurrentWindow
    repeat = 0
    delay = 0
end

begin
    remote = mceusb
    prog = irxevent
    button = Forward
    config = Key Right CurrentWindow
    repeat = 0
    delay = 0
end

begin
    remote = mceusb
    prog = irxevent
    button = Play
    config = Key P CurrentWindow
    repeat = 0
    delay = 0
end

begin
    remote = mceusb
    prog = irxevent
    button = VolDown
    config = Key [ CurrentWindow
    repeat = 0
    delay = 0
end

begin
    remote = mceusb
    prog = irxevent
    button = Stop
    config = Key Escape CurrentWindow
    repeat = 0
    delay = 0
end

begin
    remote = mceusb
    prog = irxevent
    button = Back
    config = Key Escape CurrentWindow
    repeat = 0
    delay = 0
end

begin
    remote = mceusb
    prog = irxevent
    button = VolUp
    config = Key ] CurrentWindow
    repeat = 0
    delay = 0
end

begin
    remote = mceusb
    prog = irxevent
    button = Five
    config = Key 5 CurrentWindow
    repeat = 0
    delay = 0
end

begin
    remote = mceusb
    prog = irxevent
    button = More
    config = Key I CurrentWindow
    repeat = 0
    delay = 0
end

begin
    remote = mceusb
    prog = irxevent
    button = Four
    config = Key 4 CurrentWindow
    repeat = 0
    delay = 0
end

begin
    remote = mceusb
    prog = irxevent
    button = OK
    config = Key Return CurrentWindow
    repeat = 0
    delay = 0
end

begin
    remote = mceusb
    prog = irxevent
    button = Up
    config = Key Up CurrentWindow
    repeat = 0
    delay = 0
end

begin
    remote = mceusb
    prog = irxevent
    button = RecTV
    config = Key R CurrentWindow
    repeat = 0
    delay = 0
end

begin
    remote = mceusb
    prog = irxevent
    button = Nine
    config = Key 9 CurrentWindow
    repeat = 0
    delay = 0
end

begin
    remote = mceusb
    prog = irxevent
    button = Three
    config = Key 3 CurrentWindow
    repeat = 0
    delay = 0
end

begin
    remote = mceusb
    prog = irxevent
    button = Enter
    config = Key Enter CurrentWindow
    repeat = 0
    delay = 0
end

begin
    remote = mceusb
    prog = irxevent
    button = Eight
    config = Key 8 CurrentWindow
    repeat = 0
    delay = 0
end

begin
    remote = mceusb
    prog = irxevent
    button = Guide
    config = Key Alt-G CurrentWindow
    repeat = 0
    delay = 0
end

begin
    remote = mceusb
    prog = irxevent
    button = Left
    config = Key Left CurrentWindow
    repeat = 0
    delay = 0
end

begin
    button = Power
    prog = irexec
    repeat = 0
    config = sudo /usr/sbin/s2ram -f --vbe_mode --pci_save
end

begin
    remote = mceusb
    prog   = irxevent
    button = TV
    config = Key W CurrentWindow
    repeat = 0
    delay = 0
end

begin
    remote = mceusb
    prog   = irxevent
    button = Record
    config = Key R CurrentWindow
    repeat = 0
    delay = 0
end

begin
    remote = mceusb
    prog   = irxevent
    button = Clear
    config = Key Backspace CurrentWindow
    repeat = 0
    delay = 0
end

begin
    remote = mceusb
    prog   = irxevent
    button = Teletext
    config = Key T CurrentWindow
    repeat = 0
    delay = 0
end

begin
    remote = mceusb
    prog   = irxevent
    button = Radio
    config = Key Escape CurrentWindow
    repeat = 0
    delay = 0
end

begin
    remote = mceusb
    prog   = irxevent
    button = Print
    config = Key Escape CurrentWindow
    repeat = 0
    delay = 0
end

# Special function keys - set jump points by updating the MythTV jump points
# table.  This must be done manually via direct SQL updates.

# update jumppoints set keylist='Alt+G' where destination='Program Guide' and hostname='mythtv01';
# update jumppoints set keylist='Alt+P' where destination='Live TV' and hostname='mythtv01';
# update jumppoints set keylist='Alt+R' where destination='Previously Recorded' and hostname='mythtv01';
# update jumppoints set keylist='Alt+M' where destination='Play music' and hostname='mythtv01';
# update jumppoints set keylist='Alt+V' where destination='MythVideo' and hostname='mythtv01';
# update jumppoints set keylist='Alt+D' where destination='Play DVD' and hostname='mythtv01';
# update jumppoints set keylist='Alt+S' where destination='Status Screen' and hostname='mythtv01';

begin
    remote = mceusb
    prog   = irxevent
    button = Videos
    config = Key Alt-V CurrentWindow
    repeat = 0
    delay = 0
end

begin
    remote = mceusb
    prog   = irxevent
    button = Pictures
    config = Key Escape CurrentWindow
    repeat = 0
    delay = 0
end

begin
    remote = mceusb
    prog   = irxevent
    button = RecTV
    config = Key Alt-R CurrentWindow
    repeat = 0
    delay = 0
end


begin
    remote = mceusb
    prog   = irxevent
    button = Music
    config = Key Alt-M CurrentWindow
    repeat = 0
    delay = 0
end

begin
    remote = mceusb
    prog   = irxevent
    button = DVD
    config = Key Alt-D CurrentWindow
    repeat = 0
    delay = 0
end

begin
    remote = mceusb
    prog   = irxevent
    button = LiveTV
    config = Key Alt-P CurrentWindow
    repeat = 0
    delay = 0
end

begin
    remote = mceusb
    prog   = irxevent
    button = Star
    config = Key Escape CurrentWindow
    repeat = 0
    delay = 0
end

begin
    remote = mceusb
    prog   = irxevent
    button = Hash
    config = Key Escape CurrentWindow
    repeat = 0
    delay = 0
end

# Unlabeled colored buttons across bottom of remote

begin
    remote = mceusb
    prog   = irxevent
    button = Red
    config = Key Alt-S CurrentWindow
    repeat = 0
    delay = 0
end

begin
    remote = mceusb
    prog   = irxevent
    button = Green
    config = Key Alt-N CurrentWindow
    repeat = 0
    delay = 0
end

begin
    remote = mceusb
    prog   = irxevent
    button = Yellow
    config = Key Alt-W CurrentWindow
    repeat = 0
    delay = 0
end

begin
    remote = mceusb
    prog   = irxevent
    button = Blue
    config = Key Escape CurrentWindow
    repeat = 0
    delay = 0
end

